#- name: Push Image
#  service: app
#  type: push
#  image_name: registry.heroku.com/tasklist/web
#  registry: registry.heroku.com
#  dockercfg_service: heroku-dockercfg-generator

- type: push
  service: app
  image_name: despinalr/tasklist
  encrypted_dockercfg_path: dockercfg.encrypted
  #dockercfg_service: tasklist_generator

#- service: app
#  command: /bin/true
#  encrypted_dockercfg_path: dockercfg.encrypted

- name: deploy
  service: heroku-deployment
#  command: heroku container:release --app despinalr/tasklist
#  command: node server.js
  command: codeship_heroku deploy /deploy heroku-deployment
  encrypted_dockercfg_path: dockercfg.encrypted
  #dockercfg_service: heroku-dockercfg-generator